# 基础环境变量：所有构建模式（含 CI）都会加载，Taro 加载顺序 .env → .env.local → .env.[mode] → .env.[mode].local
# 只放【非敏感】且前端构建期需要的 TARO_APP_* 变量。
# 敏感值（如 JWT_SECRET）仍放在被 .gitignore 忽略的 .env.production / .env.development，不进版本库。
#
# 为什么必须有这个文件：CI 构建拿不到被忽略的 .env.production，
# 若 TARO_APP_CLOUD_ENV 缺失，Taro.cloud.init({ env: undefined }) → 云函数调用打不到环境
# → 微信登录静默无响应（曾导致审核驳回）。此处兜底，保证任何环境都能拿到云开发环境 ID。
TARO_APP_CLOUD_ENV="dev-4gs517j09b896e44"
